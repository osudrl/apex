import numpy as np

def eval_policy():
    # Load from policy file

    # Loop through all params, for each set a high and a low, testing each.
    # Then reset the param and move to the next. Measure time to death, and
    # a bool representing if it made it to the end.


    # Print a report to the screen and the filesystem. Show death percent and
    # experiment data.
    pass